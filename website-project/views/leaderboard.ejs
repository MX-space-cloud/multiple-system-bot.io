<div class="container my-5">
  <div class="row">
    <div class="col-lg-8 mx-auto">
      <div class="card shadow-lg border-0 rounded-lg">
        <div class="card-header bg-primary text-white">
          <h2 class="text-center mb-0">🏆 Bot Leaderboards</h2>
        </div>
        <div class="card-body">
          <div class="text-center mb-4">
            <p class="lead">Check out the top performers across different games!</p>
          </div>
          
          <div class="mb-4">
            <h3 class="border-bottom pb-2">Game Categories</h3>
            <div class="row mt-3">
              <% gameTypes.forEach((gameType) => { %>
                <div class="col-md-4 mb-3">
                  <div class="card h-100">
                    <div class="card-body text-center">
                      <h5 class="card-title">
                        <% if (gameType === 'collaborative') { %>
                          <i class="fas fa-users text-primary me-2"></i> Raid Battles
                        <% } else if (gameType === 'trivia') { %>
                          <i class="fas fa-question-circle text-success me-2"></i> Trivia
                        <% } else if (gameType === 'rps') { %>
                          <i class="fas fa-hand-rock text-danger me-2"></i> Rock Paper Scissors
                        <% } %>
                      </h5>
                      <p class="card-text">
                        <% if (gameType === 'collaborative') { %>
                          Team up with other players to defeat powerful raid bosses!
                        <% } else if (gameType === 'trivia') { %>
                          Test your knowledge with challenging trivia questions!
                        <% } else if (gameType === 'rps') { %>
                          Challenge the bot to a game of rock paper scissors!
                        <% } %>
                      </p>
                      <a href="/leaderboard/<%= gameType %>" class="btn btn-primary">View Leaderboard</a>
                    </div>
                  </div>
                </div>
              <% }); %>
            </div>
          </div>
          
          <div class="border-top pt-4">
            <h3 class="border-bottom pb-2">How to Share Your Achievements</h3>
            <div class="mt-3">
              <p>You can share your achievements on the leaderboard with friends using our social sharing feature!</p>
              <ol>
                <li>Use the <code>/raid leaderboard</code> command in Discord</li>
                <li>Click the "Share" button on the leaderboard message</li>
                <li>The bot will generate a unique shareable link</li>
                <li>Share the link with friends or on social media</li>
              </ol>
              <div class="alert alert-info">
                <i class="fas fa-info-circle me-2"></i> Shared leaderboards are viewable by anyone with the link, even if they don't have Discord!
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>